

/**
 * TBD - opens an IPv4/TCP socket, and listens to traffic on port 12345
 *    (Any address).
 *
 * Waits for the first incoming connection, prints the IP address of the
 *   connected client, waits for an incoming message from the connected
 *   client, prints the received message, and then closes the connection.
 */
